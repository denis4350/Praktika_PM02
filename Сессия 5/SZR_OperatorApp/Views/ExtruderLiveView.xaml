<UserControl x:Class="SZR_OperatorApp.Views.ExtruderLiveView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:converters="clr-namespace:SZR_OperatorApp.Converters">

    <UserControl.Resources>
        <converters:SeverityToColorConverter x:Key="SeverityToColorConverter"/>

        <Style x:Key="ZoneCardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="12"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <Style x:Key="TelemetryValueStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <Border Grid.Row="0" Background="#2C3E50" CornerRadius="8" Padding="15" Margin="0,0,0,15">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="⚡" FontSize="24" Margin="0,0,10,0"/>
                <TextBlock Text="Экструдер LIVE" FontSize="18" FontWeight="Bold" Foreground="White"/>
                <TextBlock Text=" | Данные в реальном времени" FontSize="13" Foreground="#BDC3C7" Margin="10,0,0,0" VerticalAlignment="Center"/>
            </StackPanel>
        </Border>

        <!-- Панель управления -->
        <Border Grid.Row="1" Background="White" CornerRadius="8" Padding="15" Margin="0,0,0,15" BorderBrush="#E0E0E0" BorderThickness="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="Партия:" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <ComboBox x:Name="BatchComboBox" Grid.Column="1" Width="200" Height="32" 
                          DisplayMemberPath="BatchNumber" SelectedValuePath="BatchNumber"
                          SelectionChanged="BatchComboBox_SelectionChanged"/>

                <Button x:Name="RefreshButton" Grid.Column="2" Content="🔄 Обновить" Width="100" Height="32"
                        Background="#2E75B6" Foreground="White" Cursor="Hand" Click="RefreshButton_Click"/>
            </Grid>
        </Border>

        <!-- Зоны экструдера -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <WrapPanel x:Name="ZonesPanel" HorizontalAlignment="Center"/>
        </ScrollViewer>

        <!-- Статус бар -->
        <Border Grid.Row="3" Background="#ECF0F1" CornerRadius="6" Padding="10" Margin="0,10,0,0">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="📡" FontSize="11" Margin="0,0,5,0"/>
                <TextBlock x:Name="StatusText" Text="Соединение установлено" Foreground="#27AE60" FontSize="11"/>
                <TextBlock x:Name="LastUpdateText" Text="" Foreground="#7F8C8D" FontSize="11" Margin="20,0,0,0"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>